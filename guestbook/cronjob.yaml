apiVersion: batch/v1
kind: CronJob
metadata:
  name: hello-world-cron
spec:
  suspend: false
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello-world
            image: alpine
            command: ["echo", "Hello, World!"]
          restartPolicy: OnFailure
